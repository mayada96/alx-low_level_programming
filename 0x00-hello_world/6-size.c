#include <stdio.h>

/**
 * main - Entry point
 * print the size task
 * Return: return 0 after printing
 */

int main(void)
{
	printf(sizeof(char),'\n',sizeof(int),'\n',sizeof(long int),'\n',sizeof(long long int),'\n',sizeof(float));
	return (0);
}
