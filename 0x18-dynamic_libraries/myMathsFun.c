#include <stdio.h>
#include "func.h"

/**
 * add - function that adds two arguments
 * @a: firat argument
 * @b: second argument
 * Return: an integer
 */
int add(int a, int b)
{
	return (a + b);
}
/**
 * sub - function that subtracts
 * @a: first argument
 * @b: second argument
 * Return: an integer
 */
int sub(int a, int b)
{
	return (a - b);
}
/**
 * mul - function that multiplies
 * @a: first argument
 * @b: second argument
 * Return: an integer
 */
int mul(int a, int b)
{
	return (a * b);
}
/**
 * div - function that divides
 * @a: first argument
 * @b: second argument
 * Return: an i nteger
 */
int div(int a, int b)
{
	return (a / b);
}
/**
 * mod - function that returns the module of two numbers
 * @a:  first argument
 * @b: second argument
 * Return: an integer
 */
int mod(int a, int b)
{
	return (a % b);
}
